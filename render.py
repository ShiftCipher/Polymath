#!/usr/bin/env python

file = open(category + ".pug", "w+")
    file.write(name);
file.close()


# CLI arg[1] --render <category_id>
    # $categoryId = <category_id>
    # def searchCategoryId(id)
        # L1 = 35
        # if (id > AggregateNode)

        # SEARCH <category_id> IN CategoryLevel
        # GENERATE <category_id>.html
        # CATEGORY TREE ROOTED
